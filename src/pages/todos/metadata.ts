import type { PageMetadata } from "../../types/page-metadata.js";

export const metadata: PageMetadata = {
    slug: "todos",
    label: "Todos",
    title: "Todos · Pagelab",
    description: "A todo list that survives reloads via localStorage.",
    accent: "#34d399",
    views: 1744,
    ms: 47,
} as const;
